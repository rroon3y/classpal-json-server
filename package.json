{
  "name": "classpal-json-server",
  "version": "1.0.0",
  "description": "JSON Server for ClassPal Assignment Tracker",
  "main": "index.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT db.json"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
